alertmanager:
  ingress:
    enabled: true
    hosts:
      - localhost
    paths:
      - /alertmanager
grafana:
  ingress:
    enabled: true
    hosts:
      - localhost
    paths:
      - /grafana
prometheus:
  ingress:
    enabled: true
    hosts:
      - localhost
    paths:
      - /prometheus
