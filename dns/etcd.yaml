apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: etcd-operator
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/helm/charts.git
    chart: etcd-operator
    path: stable/etcd-operator
    targetRevision: main
  destination:
    server: https://kubernetes.default.svc
    namespace: etcd-operator
  syncPolicy:
    syncOptions:
    - CreateNamespace=true
    - ServerSideApply=true
    automated:
      prune: true
      selfHeal: true
